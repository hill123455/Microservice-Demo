# serivce name
spring: 
  application: 
    name: user-service
    
server: 
  port: 8000

# eureka server url
eureka: 
  client: 
    service-url:
      default-zone: http://localhost:8761/eureka